import { Input } from "postcss"
import React, { useEffect } from "react"

const Inputs = (props: { }) => {
    useEffect(() => {
        function returnInputs() {
            

            return 
        }
        returnInputs();
    }, [props.setInputs, props]);
};

export default Inputs