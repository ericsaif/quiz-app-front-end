
const config = {
  // Use the object syntax for plugins
  // Next.js often prefers this format
  plugins: {
    tailwindcss: {}, // Just the plugin name as key, options object as value
    autoprefixer: {}, // Just the plugin name as key, options object as value
  },
};

export default config;